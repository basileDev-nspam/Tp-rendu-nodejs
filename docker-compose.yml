services:
  mongo:
    image: mongo:latest
    container_name: mongo-local
    restart: unless-stopped
    ports:
      - "27017:27017"
    env_file: .env
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
